# Non-secret BGE-M3 embedding worker profile.
RAG_WORKER_NAME=embedder
RAG_POSTGRES_DSN=postgresql+asyncpg://rag_embedder:${POSTGRES_EMBEDDER_PASSWORD}@postgres:5432/rag_ingestion
# Embeddings are handed to the indexer; this worker does not access Qdrant.
