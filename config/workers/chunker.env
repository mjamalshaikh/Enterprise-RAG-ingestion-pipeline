# Non-secret chunking worker profile.
RAG_WORKER_NAME=chunker
RAG_POSTGRES_DSN=postgresql+asyncpg://rag_chunker:${POSTGRES_CHUNKER_PASSWORD}@postgres:5432/rag_ingestion
# This worker does not access Qdrant; do not inject a Qdrant credential.
