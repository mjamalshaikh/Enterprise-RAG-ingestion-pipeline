# Non-secret transactional outbox publisher profile.
RAG_WORKER_NAME=outbox-publisher
RAG_POSTGRES_DSN=postgresql+asyncpg://rag_outbox_publisher:${POSTGRES_OUTBOX_PUBLISHER_PASSWORD}@postgres:5432/rag_ingestion
# This worker does not access Qdrant; do not inject a Qdrant credential.
